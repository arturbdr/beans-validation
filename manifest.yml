---
applications:
  - name: beans-validation
    memory: 712M
    instances: 1
    random-route: true
    path: ./target/beanvalidation.jar
    buildpacks:
      - https://github.com/cloudfoundry/java-buildpack.git
    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ }}'
